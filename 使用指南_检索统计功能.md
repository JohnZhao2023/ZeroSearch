# ZeroSearch 检索统计功能使用指南

## 功能简介

我们为 ZeroSearch 项目添加了检索统计功能，用于统计强化学习训练过程中的检索行为，特别是**迭代次数和平均每轮检索返回内容长度的关系**。

## 核心功能

### 统计指标

对于每个训练/验证样本，系统会自动统计：

1. **检索次数**: 从输入进入到检索停止的总检索次数
2. **总检索返回内容长度**: 所有检索返回内容的总字符数  
3. **平均每轮检索返回内容长度**: 总检索返回内容长度 / 检索次数

### 自动收集

- 训练阶段：每个batch自动收集
- 验证阶段：每个batch自动收集
- 记录元数据：训练步数、批次索引、阶段信息等

### 自动导出

训练/验证结束时自动生成两种格式文件：

- **Excel文件** (`.xlsx`): 包含原始数据、聚合统计、检索次数分布三个工作表
- **JSON文件** (`.json`): 包含所有原始统计数据

## 快速开始

### 1. 运行训练

按照原有方式运行训练，无需修改任何参数：

```bash
# 示例：GRPO训练
bash train_grpo.sh NUM_GPUS_PER_NODE 4 \
    MODEL_PATH Qwen2.5-3B-Instruct \
    DATA_PATH ZeroSearch_dataset \
    TOTAL_STEPS 203 \
    IP localhost \
    SEARCH_MODE simulate_prompt \
    SIMULATION_LLM Qwen2.5-14B-Instruct \
    START_THRESHOLD 0 \
    END_THRESHOLD 0.5 \
    SEARCH_ENGINE google \
    MAX_TURNS 5 \
    TOPK 5
```

### 2. 查看实时输出

训练过程中，每个batch处理完成后会打印统计信息：

```
=== 检索统计信息 ===
样本 0: 检索次数=3, 总长度=1542, 平均长度=514.00
样本 1: 检索次数=2, 总长度=1028, 平均长度=514.00
样本 2: 检索次数=4, 总长度=2056, 平均长度=514.00
...
```

### 3. 获取输出文件

训练结束后，在输出目录下会自动生成统计文件：

```
<训练输出目录>/retrieval_stats/
├── retrieval_stats_20260123_143025.xlsx
└── retrieval_stats_20260123_143025.json
```

## 输出文件说明

### Excel文件结构

#### 工作表1: 原始数据

| 列名 | 说明 |
|-----|------|
| timestamp | 记录时间戳 |
| sample_index | 样本在批次中的索引 |
| retrieval_count | 该样本的检索次数 |
| total_retrieval_length | 总检索返回内容长度（字符数） |
| avg_retrieval_length | 平均每轮检索返回内容长度 |
| global_step | 全局训练步数 |
| batch_idx | 批次索引 |
| phase | 阶段（training/validation） |

#### 工作表2: 聚合统计（按检索次数分组）

| 列名 | 说明 |
|-----|------|
| retrieval_count | 检索次数 |
| total_length_mean | 总长度的平均值 |
| total_length_std | 总长度的标准差 |
| total_length_min | 总长度的最小值 |
| total_length_max | 总长度的最大值 |
| sample_count | 该检索次数的样本数量 |
| avg_length_mean | 平均长度的平均值 ⭐核心指标 |
| avg_length_std | 平均长度的标准差 |
| avg_length_min | 平均长度的最小值 |
| avg_length_max | 平均长度的最大值 |

⭐ **`avg_length_mean`** 是最核心的统计指标，表示不同检索次数下的平均每轮检索返回内容长度

#### 工作表3: 检索次数分布

| 列名 | 说明 |
|-----|------|
| retrieval_count | 检索次数 |
| 样本数量 | 具有该检索次数的样本数量 |

## 数据分析示例

### 使用Python分析

```python
import pandas as pd
import matplotlib.pyplot as plt

# 读取聚合统计
df_agg = pd.read_excel('retrieval_stats_20260123_143025.xlsx', 
                        sheet_name='聚合统计')

# 绘制迭代次数与平均检索长度的关系
plt.figure(figsize=(10, 6))
plt.errorbar(df_agg['retrieval_count'], 
             df_agg['avg_length_mean'],
             yerr=df_agg['avg_length_std'],
             fmt='o-', capsize=5, capthick=2)
plt.xlabel('检索次数（迭代次数）', fontsize=12)
plt.ylabel('平均每轮检索返回内容长度', fontsize=12)
plt.title('迭代次数与平均检索长度的关系', fontsize=14)
plt.grid(True, alpha=0.3)
plt.savefig('retrieval_analysis.png', dpi=300, bbox_inches='tight')
plt.show()

# 打印统计表格
print(df_agg[['retrieval_count', 'avg_length_mean', 'sample_count']])
```

### 使用Excel分析

1. 打开生成的Excel文件
2. 选择"聚合统计"工作表
3. 选中 `retrieval_count` 和 `avg_length_mean` 两列
4. 插入 → 图表 → 折线图/散点图
5. 即可看到迭代次数与平均检索长度的关系

## 多次实验对比

由于每次训练都会生成带时间戳的独立文件，您可以：

1. 运行多次实验，每次使用不同的参数（如不同的MAX_TURNS、THRESHOLD等）
2. 收集所有实验的Excel文件
3. 对比不同实验设置下的统计结果

示例对比脚本：

```python
import pandas as pd
import matplotlib.pyplot as plt

# 读取多个实验的数据
experiments = {
    'MAX_TURNS=3': 'retrieval_stats_exp1.xlsx',
    'MAX_TURNS=5': 'retrieval_stats_exp2.xlsx',
    'MAX_TURNS=7': 'retrieval_stats_exp3.xlsx',
}

plt.figure(figsize=(12, 6))

for exp_name, file_path in experiments.items():
    df = pd.read_excel(file_path, sheet_name='聚合统计')
    plt.plot(df['retrieval_count'], df['avg_length_mean'], 
             marker='o', label=exp_name)

plt.xlabel('检索次数', fontsize=12)
plt.ylabel('平均每轮检索返回内容长度', fontsize=12)
plt.title('不同MAX_TURNS设置下的检索长度对比', fontsize=14)
plt.legend()
plt.grid(True, alpha=0.3)
plt.savefig('experiments_comparison.png', dpi=300, bbox_inches='tight')
plt.show()
```

## 技术细节

### 统计收集位置

统计数据在以下位置收集：

- **文件**: `ZeroSearch/llm_agent/generation.py`
- **方法**: `LLMGenerationManager.run_llm_loop()`
- **时机**: 每次检索操作执行后

### 数据流程

```
训练/验证开始
    ↓
每个batch处理
    ↓
调用 run_llm_loop()
    ↓
多轮检索循环
    ├─ 每次检索后记录内容长度
    └─ 累计统计信息
    ↓
返回 retrieval_stats
    ↓
添加到 stats_collector
    ↓
训练/验证结束
    ↓
自动导出 Excel 和 JSON
```

### 内容长度计算

- 使用Python的 `len()` 函数计算字符串长度
- 包括检索返回的所有内容（文档标记、文本等）
- 单位：字符数

## 常见问题

### Q: 统计数据全为0怎么办？

**A**: 这可能是因为：
- 模型从未执行检索操作（全部直接回答）
- 配置中 `trainer.do_search=False`

这是正常现象，说明模型判断不需要检索。

### Q: 如何只统计验证集？

**A**: 使用验证模式运行：
```bash
# 在配置中设置 val_only=True
```

### Q: 可以自定义输出路径吗？

**A**: 可以，修改 `ray_trainer.py` 中的初始化代码：
```python
self.stats_collector = get_global_collector(
    output_dir='你的自定义路径'
)
```

### Q: Excel文件需要什么依赖？

**A**: 需要安装 `openpyxl`:
```bash
pip install openpyxl
```

### Q: 如何清空历史统计？

**A**: 删除输出目录下的文件或调用：
```python
from llm_agent.stats_collector import get_global_collector
collector = get_global_collector()
collector.clear()
```

## 验证测试

我们提供了测试脚本验证功能：

```bash
cd ZeroSearch
python test_retrieval_stats_simple.py
```

测试通过会显示：
```
✓ 所有测试通过!

统计功能已正确实现，包括:
  1. 检索次数统计
  2. 总检索返回内容长度统计
  3. 平均每轮检索返回内容长度计算
  4. 数据导出为JSON/Excel格式
```

## 总结

该功能完全集成到训练流程中，无需额外操作即可自动收集和导出统计数据。您可以：

1. ✅ 统计每个样本的检索次数
2. ✅ 统计总检索返回内容长度
3. ✅ 计算平均每轮检索返回内容长度
4. ✅ 按检索次数分组聚合统计
5. ✅ 导出Excel和JSON格式
6. ✅ 支持多次实验对比

现在您可以开始运行强化学习训练，系统会自动收集和导出所需的统计数据！

---

如有任何问题，请参考 `RETRIEVAL_STATS_README.md` 获取更多技术细节。
